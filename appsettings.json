{
    "ConnectionStrings": {
        "DefaultConnection": "Server=DESKTOP-TM6EI00;Database=InventoryOrderDb;Trusted_Connection=True;TrustServerCertificate=True"
        // Stores the main database connection string used by the application
    },
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            // Sets the default logging level for the whole application

            "Microsoft.AspNetCore": "Warning"
            // Limits ASP.NET Core framework logs to warnings and above
        }
    },
    "AllowedHosts": "*"
    // Allows the app to respond to requests from any host
}